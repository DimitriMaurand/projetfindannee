{% extends 'base.html.twig' %}

{% block title %}CategorieProduit{% endblock %}

{% block body %}
      <h2 class="text-xl font-bold text-center m-4">Catégorie de produit</h2>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_produit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie_produit.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_produit_index') }}"><button  class="  w-auto bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-md">Retour</button></a>

    <a href="{{ path('app_categorie_produit_edit', {'id': categorie_produit.id}) }}"><button class="btn w-20 bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 ml-2 rounded-md m-2">editer</button></a>

    {{ include('categorie_produit/_delete_form.html.twig') }}
{% endblock %}
