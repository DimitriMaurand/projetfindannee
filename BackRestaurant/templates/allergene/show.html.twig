{% extends 'base.html.twig' %}

{% block title %}Allergene{% endblock %}

{% block body %}
    <h2 class="text-xl font-bold text-center m-4">Allergène</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ allergene.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ allergene.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_allergene_index') }}"><button  class="  w-auto bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-md">Retour</button></a>

    <a href="{{ path('app_allergene_edit', {'id': allergene.id}) }}"><button class="btn w-20 bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 ml-2 rounded-md m-2">editer</button></a>

    {{ include('allergene/_delete_form.html.twig') }}
{% endblock %}
